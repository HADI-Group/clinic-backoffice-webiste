server {
    listen 80;

    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA routing (IMPORTANT)
    location / {
        try_files $uri $uri/ /index.html;
    }
    location /api/ {
        proxy_pass http://clinic-api-staging:3000/;
    }
}